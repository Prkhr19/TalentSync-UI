import { Navigate } from 'react-router-dom'
import { ROUTES } from '../../Routes/Routes'

const CandidateReferrals = () => <Navigate to={ROUTES.APPPLICATION_STATUS} replace />

export default CandidateReferrals
